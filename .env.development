VITE_APP_ENV=DEV
# 留空 → axios baseURL 走相對路徑 /api，由 vite proxy 轉發到 8082（同源、無 CORS、不受 port 跳號影響）
# 要直連後端時改回 http://localhost:8082（會走跨 origin + CORS，origin 需與後端白名單一致）
VITE_API_BASE=http://localhost:8082
VITE_DEV_BYPASS_AUTH=true
